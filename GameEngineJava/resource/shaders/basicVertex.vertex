#version 330

layout (location = 0) in vec3 position;

out vec3 colour;
uniform float uniformFloat;

void main() {
    colour = clamp( position, 0.0, uniformFloat );
    gl_Position = vec4( position, 1.0 );
}